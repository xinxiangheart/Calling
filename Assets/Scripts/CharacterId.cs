/// <summary>
/// 可选角色的标识。
///
/// 数值会被写进存档，所以已经有项的**数值不能改**，新角色只能加在末尾。
/// </summary>
public enum CharacterId
{
    Warrior,    // 格雷
    Rogue,      // 莉拉
    Alchemist,  // 诺姆
    Gambler     // 卡珊德拉
}